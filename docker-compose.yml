version: "3"
services:
    app:
        image: golang:1.13
        working_dir: /work
        tty: true
        ports:
            - 8001:8001
        volumes:
            - ./:/work
        command: /bin/bash
        environment:
            PORT: 8001
